name: Aggiorna date prove libere Mugello

on:
  schedule:
    # Ogni 8 ore. Sintassi cron: minuto ora giorno mese giorno-settimana (UTC)
    - cron: "0 */8 * * *"
  workflow_dispatch: {}  # permette anche l'avvio manuale dal tab Actions

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Installa dipendenze
        run: pip install -r requirements.txt

      - name: Installa browser per Playwright
        run: playwright install --with-deps chromium chromium-headless-shell

      - name: Esegui gli scraper
        run: python main.py

      - name: Commit del nuovo data.json (se cambiato)
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "Aggiornamento automatico date prove libere"
          file_pattern: docs/data.json
